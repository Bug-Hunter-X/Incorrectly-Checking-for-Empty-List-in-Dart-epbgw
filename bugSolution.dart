```dart
List<int> numbers = [1, 2, 3, 4, 5];

// Correct way to check for an empty list
if (numbers.isEmpty) {
  print('List is empty');
} else {
  print('List is not empty');
}

// Another example with an empty list
List<int> emptyNumbers = [];
if (emptyNumbers.isEmpty) {
  print('List is empty');
} else {
  print('List is not empty');
}
```